#pragma once

#include <vector>
#include <random>

using namespace std;

// Neste caso, declaramos a função que gera uma matriz aleatória;
// Isso faz com que possamos criar matrizes de teste;
vector<vector<int>> geraMatrizAleatoria(int linhas_, int colunas_);